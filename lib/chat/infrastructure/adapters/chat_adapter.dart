import '../../domain/entities/contact.dart';

class ChatAdapter {
  void addContact(Contact contact) {
    // Implementación para agregar contacto (puede ser una llamada a una API, base de datos, etc.)
  }
}
